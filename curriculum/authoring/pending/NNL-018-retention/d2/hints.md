# 分级提示（待审）

- H1：查阅 NumPy reshape 的 `-1` 推断和零维度规则；显式 I/O 可以避免空批的歧义。
- H2：先区分“每个输入自己的梯度”和“所有样本共用参数的梯度”；想一想哪些轴代表同一个参数被重复使用。
- H3：画出单微批的 shape 流，再决定跨 chunk 保留哪些量。先验证两个不同大小微批与合并微批的一致性，再处理向量、空批和不连续视图。不要通过增加一次平均让测试看起来合理。

终止追问条件：候选人能解释共享参数梯度的累加来源、归一化责任、零长度行为，并用自造反例证实分块不变性；否则换成两个标量样本的具体例子，不重复同义追问。
