# NNL-018 分级提示

## H1 · 官方文档 / 单一语法

查看 NumPy reshape、swapaxes 的形状语义。

## H2 · 概念方向

对每个输出元素写依赖关系，区分共享参数与每个样本独有的输入。

## H3 · 结构步骤

先检查三组形状，处理单向量与空 batch，再将前导轴视为样本索引，分别计算三个梯度并还原输入形状。

## 复测方向

关闭旧实现后更换输入组织重写；再用不同边界调试。方向不是已通过的 retention 证据。H4/H5 只可在明确授权的私人新变式中进行。
