# ATT-019 分级提示

## H1

先核对题面接口、归约轴、允许的 API；阅读题面末尾的技术来源。

## H2

计算 `QKᵀ/√D`，在 softmax 之前屏蔽非法位置，再加权求和。若同时要求因果关系与 padding，允许矩阵是两者的交集；如果 padding query 也必须为零，应把 query 有效性纳入矩阵。投影后的 `view/transpose` 要逐步写 shape。

## H3

先实现最小非退化输入，再补状态和边界。验证题面列出的空值、同分、mask、状态与输入不变性。

## 口述追问

改变一个输入规模或边界条件后，公式和复杂度是否仍成立？给出可复现的小例子。

## 复测方向

D+2：关闭旧作答，改成显式状态/批次输入，重新独立实现。D+7：用本题最容易混淆的定义设计一个反例，再验证原实现。当前未提供已验证的独立复测资产，不据此自动授予 retention。
