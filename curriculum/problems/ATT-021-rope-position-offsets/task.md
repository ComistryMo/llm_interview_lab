# ATT-021 · RoPE：旋转布局与位置偏移

资料映射：AI04。这是原创训练任务，不代表某公司必考题。

推荐准备：[ATT-007](../ATT-007-rope/task.md)。推荐顺序不等于硬解锁条件；实际解锁条件见课程入口。

## 接口

```python
def rope_positions(x, position_ids, base=10000.0): ...
```

## 题目要求

- X[B,H,T,D]，D 正偶数
- int64 position_ids[B,T] 可按样本不同，base>1。相邻偶/奇坐标成对旋转，频率 base**(-2i/D)，角度为真实位置乘频率，返回同 shape/dtype/device。半精度三角计算升 float32，float64 保留
- 不旋转 V，不混用 half-split 布局。position=0 恒等，向量范数和共同位置平移后的 QK 内积不变
- O(BHTD)。奇数 D 或错误位置 shape 抛 ValueError。

## 共同约定

输入不允许被原地修改（接口明确的状态更新除外）。除题目指定的空输入与异常，输入形状合法、数值有限；不要增加与本题目标无关的校验。张量位于同一设备；浮点使用 float32/float64，保持输出设备与明确的梯度路径；禁止直接调用题目要求手写的整层/算法。

## 验收与复盘

可在编辑器中自行构造输入并运行，也可运行公开测试。测试通过只是实现证据，不等于掌握。完成后解释 shape、复杂度、一个失败边界和测试依据；提示见 hints.md。

## 口述与追问

- 相邻配对与 half-split 布局能否直接共用权重？
- 两侧位置共同加常数后点积为什么保持？
- 从位置 4 开始的分块怎样与全量前向对齐？
- 为什么不能使用 FP16 累加长序列位置？

## 来源

- [技术定义 1](https://arxiv.org/html/2104.09864v5)
