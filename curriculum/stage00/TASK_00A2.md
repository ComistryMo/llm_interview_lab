# Task 00A-2：筛选困难样本

前置条件：Task 00A-1 至少达到 `implemented`。

## 目标

从样本列表中筛选错误次数不少于阈值的样本。

## 必须处理

- 阈值必须大于 0；
- 字段缺失；
- predictions 为空；
- 保持原始顺序；
- 返回新列表；
- 不修改输入和内部字典；
- 复用 00A-1，而不是复制计数逻辑。

## 新语法

- list of dict；
- `.append`；
- `in` / `not in`；
- 浅拷贝与“只读引用”的初步概念；
- 函数复用。
