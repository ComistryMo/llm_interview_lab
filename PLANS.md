# ExecPlan 规则

ExecPlan 只用于以下情况：

- 新增一个持续两周以上的完整训练阶段；
- 跨 5 个以上文件修改课程或测试基础设施；
- 重构仓库状态模型；
- 建立公开作品仓库的迁移流程。

普通 Task、代码审查、提示和复测不创建 ExecPlan。

## ExecPlan 最小模板

在 `plans/active/<name>.md` 中记录：

1. 目标与可观察结果；
2. 当前仓库事实；
3. 范围与明确不做的内容；
4. 分阶段里程碑；
5. 每阶段测试命令；
6. 风险、回退和停止条件；
7. 决策日志；
8. 当前进度；
9. 最终复盘。

计划必须能够让一个没有历史会话记忆的 Codex，仅凭仓库和该文件继续工作。
