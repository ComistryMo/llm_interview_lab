# v0.4.0-alpha.4 Quiet Forge 桌面工作台重设计

## 目标与可观察结果

本计划把当前偏卡片后台的桌面界面逐步收敛为安静、精确、适合长时间训练的本地工作台。本轮只实施 S0 与 S1：冻结真实交互合同，建立主题、首批共享组件与 Shell。页面业务重排留给后续切片。

可观察结果：

- Light/Dark 具有一致的 canvas、chrome、surface、文字、边界和状态层级；
- Shell 在 900×620 使用紧凑图标栏，在 1040px 以上使用标准栏，在 1400px 以上才展开文字侧栏；
- 全应用只有一个页面 H1；Windows/Linux 不再显示突兀的窗口内灰色菜单条，同时 About、Settings、Quit、Continue、Run Tests 仍可达；
- Command Palette、Toast、Codex approval 保持既有事实来源和测试锚点；
- 首批控件具备 hover、press、focus、disabled/busy 与可访问名称；
- screenshot/test 可用非持久化参数固定字体比例和 motionScale=0；
- 新 QML、SVG 与许可同步进入源码包及 Windows/macOS deploy 配置。

## 当前仓库事实

- 实际基线：`d7631264c344ddf9197c51f64a0fe2088817c736`；
- 实施分支：`feat/v0.4.0-alpha.4-premium-ui`；
- 独立 worktree：`E:\\llm-lab-wt-alpha4`；
- 主工作树中两个既有未知文件保持不动，本 worktree 创建时干净；
- 当前桌面端为 PySide6 / Qt Quick，页面通过 `StackLayout` 常驻；
- 共享组件仅有 `LabCard.qml` 与 `StatusPill.qml`；
- `Main.qml` 同时承担主题、Shell、菜单、Command Palette、Toast 与 Codex approval；
- 两份 deploy spec 显式枚举 QML；package-data 当前仅递归包含 QML、非递归包含 resources；
- 当前截图 CLI 已有 window size/theme/page 参数，但没有独立 font/motion 展示参数。

## 范围

### S0：冻结基线与交互合同

- 记录 Shell、导航、菜单命令、关键 objectName、approval、Toast、页面生命周期；
- 保留现有截图及 manifest，不覆盖旧 provenance；
- 定义本轮 token、响应式断点、组件状态和菜单等价合同；
- 为 S1 增加聚焦的设计系统合同测试。

### S1：Theme、基础组件与 Shell

- 新增本切片实际使用的 `AppTheme`、`LabText`、`LabButton`、`LabIconButton`、`LabSurface`、`LabDivider`、`LabBusyIndicator`、`LabTextField`、`LabScrollBar`、`LabDialog` 与 `NavItem`；
- 升级 `LabCard` 与 `StatusPill`，保留旧属性兼容；
- 将 `Main.qml` 收敛为统一 Shell，保留 `StackLayout` 和所有既有业务连接；
- 建立 compact / standard / wide 三种唯一 layoutMode；
- 重排 Command Palette、Toast、Codex approval 的展示和焦点；
- 增加仅用于 screenshot/smoke 的 font scale 与 motion scale 参数，不写入用户设置；
- 同步 Windows/macOS deploy spec 与 package-data；
- 生成 Home、Settings、Connections 的小矩阵截图，作为 S1 证据而非最终发布截图。

`LabToolTip` 与 `PageScaffold` 没有被 S1 Shell 直接调用，因此没有为清单完整性创建；提示行为由按钮组件包装，页面骨架留到实际逐页迁移时再判断。

## S0 冻结合同

| 区域 | 本切片冻结的事实 | S1 允许改变 |
| --- | --- | --- |
| Shell | 9 个常驻 `StackLayout` 页面；切页不得销毁草稿、滚动与会话 | Chrome、导航层级、Topbar 与响应式外观 |
| 命令 | About、Settings、Quit、Continue、Run Tests 必须可达 | Windows/Linux 转入 Command Palette；macOS 保留原生菜单 |
| 自动化 | Command Palette、Toast、Codex approval 与 `moreNavigationButton` 的 objectName | 视觉容器、间距、焦点和状态表达 |
| 安全 | Approval 的请求、详情、批准与拒绝仍由 Controller 提供 | Banner/Dialog 的低噪声呈现 |
| 页面 | Home、Exercise、Coach、Interview 的领域动作和状态来源不变 | 只通过兼容 palette 接收新主题 |
| 截图 | 旧截图 manifest 不覆盖、不混入新提交证据 | 证据目录可使用非持久化 font/motion override |

## 明确不做

- 不修改 Catalog、DAG、Workspace、Events、mastery、retention；
- 不改变面试计时、暂停恢复、答案锁定、grader SHA 或评分证据；
- 不改变 Provider/Codex/consent/Profile 隔离语义；
- 不做 frameless 窗口、WebView、Controller 重写或全局状态框架；
- 不重排 Onboarding/Home/Learn/Exercise/Coach/Interview 等页面业务结构；
- 不批量替换全部旧 palette、字号或 Rectangle；
- 不新增假按钮、假进度、假 AI 状态或后端不存在的动作；
- 不覆盖 authoritative screenshot manifest；
- 不运行全量 pytest 或跨平台发布构建。

## 行为合同

- 保留：`commandPalette`、`commandPaletteSearch`、`commandPaletteList`、`globalToast`、`codexApprovalBanner`、`codexApprovalDetails`、`moreNavigationButton` 及 approval 按钮锚点；
- 保留 StackLayout 页面实例，切页不销毁草稿、搜索、滚动和会话；
- Windows/Linux 隐藏菜单条前，About、Settings、Quit、Continue、Run Tests 必须通过 Command Palette、页面或快捷键等价可达；macOS 保留原生菜单语义；
- QML 只展示 Controller/Application 状态，不从中文字符串重新推断领域状态；
- Reduced Motion 与截图 motionScale 只影响展示，不持久化、不进入领域事件；
- 新图标只使用项目自有或明确可再分发的单色 SVG，并附许可。

## 验证命令

本轮只运行：

```text
.venv\\Scripts\\python.exe -c "import PySide6; print(PySide6.__version__)"
.venv\\Scripts\\python.exe -m compileall -q src/llm_interview_lab
.venv\\Scripts\\python.exe -m pytest -q <S1 定向测试集合>
.venv\\Scripts\\python.exe -m llm_interview_lab.desktop.main --smoke-test --window-size 900x620
git diff --check
```

截图仅覆盖 Home、Settings、Connections 的 900×620 与 1280×800、Light/Dark，并由人工查看。若本环境无法动态加载 PySide6，必须明确标记未验证，不能用静态检查冒充 UI smoke。

### S1 实际验证记录

使用 Python 3.11，并显式令 `PYTHONPATH=E:\llm-lab-wt-alpha4\src`，避免共享 editable 环境加载另一工作树：

```text
python -m pytest -q tests/infrastructure/test_desktop.py::test_demo_controller_exposes_every_page_and_keeps_demo_settings_deterministic tests/infrastructure/test_desktop.py::test_qml_offscreen_smoke_and_version_do_not_need_a_profile tests/infrastructure/test_onboarding_qml_hotfix.py::test_main_toast_is_top_right_and_named tests/infrastructure/test_alpha3_truthful_ux.py::test_desktop_entry_accepts_explicit_window_size_and_role_step tests/infrastructure/test_desktop_design_system.py
# 21 passed in 6.44s

python -m llm_interview_lab.desktop.main --smoke-test --window-size 900x620 --screenshot-font-scale 1.0 --screenshot-motion-scale 0.0
# status=ok; first_window_ms=2305; provider_probe=lazy; codex_probe=lazy

python -m compileall -q src/llm_interview_lab scripts
# passed

python -c "parse every desktop SVG with xml.etree.ElementTree"
# 13 SVG files parsed

git diff --check
# passed；仅出现 Git 的 LF/CRLF 工作区提示
```

截图通过 `QT_QPA_PLATFORM=windows` 生成 12 张原生中文证据，并逐张人工查看；offscreen 只承担动态 smoke，不冒充字体视觉证据。未运行全量 pytest、Windows/macOS Artifact 构建、macOS 动态 smoke 或 Release CI。

## 风险与回退

- 旧页面大量依赖 `window.colors`：AppTheme 必须提供兼容 palette，逐页迁移；
- 组件提取可能破坏测试锚点：objectName 留在真实可观察/可交互对象；
- 新 QML/SVG 可能漏打包：同一提交同步两份 deploy spec 与 package-data；
- 动效可能使截图不确定：截图模式强制 motionScale=0；
- compact 模式可能隐藏主动作或 approval：出现即停止并回退 Shell 布局；
- 任何领域状态需求、binding loop、持续 QML warning、only-offscreen pass 或来源不明并行改动都触发停止。

## 决策日志

- 2026-08-31：采用 Quiet Forge 方向，借鉴成熟桌面工具的分栏、低噪声和渐进披露，不复制品牌外观。
- 2026-08-31：从 `d763126` 建立独立 worktree，避免触碰主工作树两个既有未知文件。
- 2026-08-31：S1 先做兼容 Theme 与 Shell，不做页面业务重排。
- 2026-08-31：保留原生窗口边框；Windows/Linux 菜单条仅在命令等价后隐藏，macOS 保留原生菜单。
- 2026-08-31：只运行定向桌面测试和小截图矩阵；完整桌面回归留到 RC 明确授权后一次执行。
- 2026-08-31：严格采用交接中的 1040/1400 断点；1280 保持 icon rail，并以 Topbar 当前页面名保证可发现性。
- 2026-08-31：兼容 palette 的 `text` 映射到 `textStrong`，避免旧页面标题被降为次级文字；主 CTA 使用显式 `accentForeground`。
- 2026-08-31：Windows 原生平台生成中文视觉证据；本机 offscreen 插件缺少可靠 CJK 字形，仅用于动态 smoke。
- 2026-08-31：Connections 只补充 1280 下两张概览卡的等宽最小布局，不改变连接行为；Home/Settings 的卡片墙留给 S2。

## 当前进度

- [x] 完整阅读 `AGENTS.md`、`PLANS.md` 与设计交接；
- [x] 核对远端 main 与 Alpha.3 修复分支；
- [x] 创建独立干净 S1 worktree 与实现分支；
- [x] 记录实际基线、关键文件和打包现状；
- [x] 完成 S0 交互合同测试；
- [x] 完成 S1 Theme 与共享组件；
- [x] 完成 Shell、Command Palette、Toast 与 approval 迁移；
- [x] 完成打包清单与 screenshot 参数；
- [x] 运行定向验证并人工审阅小矩阵截图；
- [x] 独立 Review 已批准，P0/P1 已修复，单一可回滚提交已准备完成。

## 最终复盘

S0/S1 已建立 Quiet Forge 语义主题、兼容 palette、11 个实际使用的基础组件、统一 Shell、命令等价入口、结构化 AI 状态、稳定截图参数与显式资源打包边界。独立代码审查提出的四项 P1 均已修复并批准；独立视觉盲审提出的 CTA 对比度 P0 已修复，最终截图复核已批准。

定向测试结果为 `21 passed`；900×620 offscreen smoke 返回 `status=ok`，首窗口约 2.3 秒，Provider 与 Codex 均延迟探测。视觉证据位于 `E:\llm-lab-alpha4-evidence-s1-windows`，覆盖 Home、Settings、Connections 的 900×620/1280×800 Light/Dark，并已人工查看。未运行全量 pytest、Windows/macOS Artifact 构建或 macOS 动态验证；它们不属于本切片。

本提交的回退点是整个 S0/S1 单一 commit。后续 S2 应逐页处理 Home/Settings 的卡片密度与内容裁切，不应回填到主题或领域状态。
