# 统一难度 AI 面试工作台 · ExecPlan

基线：main `0e49ce61fc14cfb4011542bc527c91a84436871a`。用户已批准实施；本计划替换此前按求职阶段分档的升级方案。

## 目标与边界

岗位决定考察方向，授权材料与回答决定切入点，简单/标准/困难决定深度和广度。新动态面试不要求或暗用 seniority；历史数据不改写。下一问优先，结束后有证据地评分；等待 AI 不计入候选时间。不改 Practice/Mastery/授权语义，不新增大型依赖、不全量回归、不打包、不发布。仅推送 main，提交使用 [skip ci]。

## 当前事实

- QML 设置、动态题池、知识检索和蓝图存在 seniority 依赖；手撕按 intern/easy、new_grad/medium、mid/hard 分档。
- 当前逐题请求同时等待详细评分和下一问；阶段依赖固定最低题数。
- 已有主题、真实作答/代码运行、流式本地语音、Profile 配置、Provider/Codex 连接复用和会话报告可复用。
- 现存未跟踪 UAT 目录、用户计划、反馈、原始图标等全部保护，不纳入提交、不读取真实 Profile。

## 三个连续切片

1. **入口与布局**：移除求职阶段 UI/新动态默认，首页面试优先、历史入口、上次配置摘要；共用带行号/高亮的原生轻量编辑器，四尺寸下题面/代码/结果布局，连续历史与稳定回答区。先验证正式页面。
2. **动态协议**：岗位技能+统一流程，下一问与评分拆分；流式正文、覆盖/时间驱动、AI 等待排除、结束逐题评分及失败恢复。旧协议保留兼容，新协议无 seniority。默认 60 分钟、标准难度，优先恢复已保存配置。
3. **内容与复盘**：深化 EGT-QB-001/002/007/011/014/019/021/025/026/028/029/030/037/048/058/068；完善 OPT-002/LOSS-014/LOSS-016/NNL-018/NNL-008/ATT-019/ATT-021/ATT-020/PT-003/PT-022/PT-023/PT-010，真实调用追问路线并关联报告到可练内容。

难度候选：easy 1–2、medium 2–4、hard 3–5（高等级优先）。经历覆盖目标 2/2–3/3 个角度，原理目标 3/3/4 个主题，不作为强制轮数。时间预算 introduction/experience/theory/coding = 5/40/25/30%。评分锚点不按难度或身份放宽。

## 验证策略

- 定向单元/集成测试：统一资格、配置重启、历史兼容、问答/评分解耦、等待计时、取消/重试、缺失题、覆盖轨迹、内容入口。
- 正式 QML：900×620、1080×680、1280×800、1440×900，深浅色，100%/125%；中文 IME、placeholder、滚动、代码与可见动作。截图使用隔离合成 Profile。
- 12 条对话轨迹（两岗位×三难度×充分/薄弱）；16 卡独立审查、12 题入口/公开契约检查。
- 有经授权可用凭证时用合成简历/JD完成 Codex/DeepSeek 各两场，记录首正文/问题完成/评分耗时。不能真实传输时如实标为未验证，不读取真实 Profile 寻找凭证。
- 每切片运行所需目标测试和 git diff --check，验收后提交；不重复全量、不触发 RC/Release CI。

## 风险、回退和停止

不得改写旧会话问题/评分，不丢回答，不把模型推理当正文，不虚构通过或完整报告。Profile/材料变更使请求失效。新旧协议通过显式标识区分；故障保留回答并提供重试，不静默新建/降级。遇真实数据损失或缺少必要外部权限停止该动作并报告。

## 决策与进度

- DEC-001：取消求职阶段是产品语义变化，不仅删控件。旧标签只用于历史兼容。
- DEC-002：本轮只维护此 ExecPlan；具体证据与完成情况持续写在此处。
- [x] 切片一实现与正式 QML 验收
- [ ] 切片二实现与动态协议验收
- [ ] 切片三实现、内容审查、报告闭环
- [ ] 集成目标验证、真实传输证据、提交推送 main

## 最终复盘

实施中；尚未宣称任何新功能或验收完成。

## 切片一证据

已移除新动态面试的求职阶段及默认值，使用显式 interaction_version=2 区分历史协议；首页以面试为主入口，加入历史与配置摘要。Practice/Interview 共用原生高亮、行号与缩进编辑器；脚本运行与公开测试分开。未修改旧 Profile 或历史 Session。

目标验证：`test_unified_interview.py` 与 `test_unified_interview_ui.py`，9 passed（44.32s）。QML 用正式 Controller、隔离合成档案，循环四尺寸×两主题×两字体比例；实际键盘输入与代码区域换栏后文本保留。截图人工检查额外发现并修复了“前序问答”按钮覆盖标题，增加直接位置断言。

截图暂存于系统临时目录 `llm-lab-unified-ui-20260908`，包含首页、配置和手撕区深浅各一张；逻辑 1280×800，Windows 150% DPR 导致 PNG 为 1920×1200。未使用 Phase 0 原型，未真实连接 AI；最终证据整理后入库。下一问/评分分离尚待切片二，不将当前过渡协议称为完整升级。
