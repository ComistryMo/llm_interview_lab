# Task 00A-1 第一次审查

## 结论

**有条件通过核心逻辑，但任务整体未通过。**

你提交的遍历和计数逻辑是正确的，且两个已运行测试通过；但是任务文字要求尚未全部满足。

## Gate 结论

常规正确性已有证据，但类型需求、学员自编异常测试、死代码清理和解释尚未全部通过，因此状态保持 `needs_revision`。本 fixture 不公开个人化分数或耗时。

## 做得好的

1. 在 H1 语法帮助范围内写出了正确主干；
2. `for i in predictions` 的使用正确，`i` 取得的是元素；
3. 计数器放在循环外，更新逻辑正确；
4. 空输入异常处理符合已有测试；
5. 算法复杂度是 `O(n)` 时间、`O(1)` 额外空间。

## 主要问题

### 1. 任务文字要求没有完全转成代码和测试

`predictions: list[int]` 只是类型标注，Python 默认不会在运行时阻止字符串、浮点数等进入列表。任务明确要求每个预测都是 int，因此需要主动检查并添加失败测试。

### 2. 有不可达死代码

```python
return nums
raise NotImplementedError(...)
```

函数执行 `return` 后立即结束，后面的 `raise` 永远不会执行。提交前应清理 starter 中的占位代码。

### 3. 变量名和复盘不够完整

`i` 常用来表示 index，但这里是 prediction 元素。使用 `prediction` 更清楚。你提交的“三个困难点”只写了一个，下一次必须把语法、测试或需求理解中的真实卡点写全；没有第三个就明确写“暂无”，不能空着。

## 你现在需要自己修改

- 删除不可达 `raise`；
- 将元素变量改为语义化名称；
- 增加 int 校验；
- 添加非 int 测试；
- 回答六个验收问题；
- 不进入下一 Task。

## 测试命令

```bash
python -m pytest tests/stage00/test_task_00a1.py -q
```

## 帮助等级

H1：只查了语法和迭代语义。这一轮仍然是有效尝试，但必须在 48 小时后无帮助重写，才能证明保留。
