# Hints

正式提示必须按 Task 和帮助等级隔离，避免学习者请求 H1/H2 时同时看到后续步骤或关键 API。

v0.1 alpha 暂不发布旧版 Stage 00 提示；AI 教练只能按 `docs/COACHING_PROTOCOL.md` 现场给当前允许等级的最小提示。B01 将为每个已验收任务分别增加 H1 文档入口、H2 概念、H3 结构和必要的 H4 片段，并确保默认不会一次读取多个等级。

完整答案永不放入本目录。
